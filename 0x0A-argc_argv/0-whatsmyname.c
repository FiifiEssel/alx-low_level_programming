#include <stdio.h>

/**
 * main- the start line for program execution
 * @argc: the count for command line instructions
 * @argv: the string array containing the command line attributes
 * Return: return 0 for sucess
 */

int main(int argc __attribute__((unused)), char *argv[]);
{
	printf("%s\n", argv[0]);
	return (0);
}
