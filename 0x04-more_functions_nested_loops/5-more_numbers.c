#include "main.h"
#include <stdio.h>

/**
 * more_numbers - will print 1 to 14 ten times
 *
 * Return: will return void
 */

void more_numbers(void)
{
	int n, n1;

	for (n1 = 1; n1 <= 10; n1++)
	{
	for (n - 0; n <= 14; n++)
	{
		if (n > 9)
			putchar((n / 10) + 48));
	putchar ((n % 10) + 48));
	}
	putchar('\n');
	}
}
